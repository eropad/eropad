const categories = [
	...Array.from<string>({length: 59}).fill('Lesbian'),
	...Array.from<string>({length: 40}).fill(''),
	...Array.from<string>({length: 1}).fill('Gay'),
];

export default categories;
